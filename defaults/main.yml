---
ar_os_secret_link_assertions: [
  "ar_os_secret_link_namespace is defined",
  "ar_os_secret_link_namespace is not none",
  "ar_os_secret_link_namespace | length",
  "'sa_name' in ar_os_secret_link_item",
  "ar_os_secret_link_item.sa_name is not none",
  "ar_os_secret_link_item.sa_name | length",
  "'secret_name' in ar_os_secret_link_item_secret",
  "ar_os_secret_link_item_secret.secret_name is not none",
  "ar_os_secret_link_item_secret.secret_name | length",
]
